package com.hrms.employee.core.enums;

public enum ConcernStatus {
    OPEN,
    IN_PROGRESS,
    RESOLVED, // Resolution provided, pending confirmation or closure
    CLOSED    // Confirmed closure
}
